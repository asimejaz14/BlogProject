# import datetime
#
#
# today = datetime.datetime.today() + datetime.timedelta(days=1)
# date_time_obj = datetime.datetime.strptime(str(today), '%Y-%m-%d %H:%M:%S.%f')
# print('Today:', date_time_obj.date())

# last_week_date = datetime.datetime.today() - datetime.timedelta(days=7)
# date_time_obj = datetime.datetime.strptime(str(last_week_date), '%Y-%m-%d %H:%M:%S.%f')
# print('Week:', date_time_obj.date())
#
# last_month_date = datetime.datetime.today() - datetime.timedelta(days=31)
# date_time_obj = datetime.datetime.strptime(str(last_month_date), '%Y-%m-%d %H:%M:%S.%f')
# print('Month:', date_time_obj.date())


data_dict = {
    'part1': 'some data 1',
    'part2': 'some data 2',
}

